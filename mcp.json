{
  "name": "top-gun-geo-lens",
  "version": "1.0.0",
  "description": "TOP GUN GEO-Lens: Brand visibility auditing across LLMs. $1.50 USDC per audit.",
  "icon": "/favicon-32x32.jpg",
  "transport": "stdio",
  "command": "node",
  "args": ["dist/index.js"],
  "env": {
    "STRIPE_SECRET_KEY": { "required": true, "description": "Stripe secret key for payment verification" },
    "STRIPE_PAYMENT_URL": { "required": true, "description": "Stripe payment link URL ($1.50 USDC)" },
    "BRAVE_SEARCH_API_KEY": { "required": false, "description": "Brave Search API key for web search" },
    "EXA_API_KEY": { "required": false, "description": "Exa neural search API key" },
    "USDC_WALLET_ADDRESS": { "required": false, "description": "USDC wallet address shown to payers" }
  },
  "tools": [
    {
      "name": "audit_brand",
      "description": "Audit a brand's visibility across LLM-indexed sources. Returns visibility score (0–100), citations, LLM index status, and GEO recommendations. Costs $1.50 USDC."
    },
    {
      "name": "get_payment_info",
      "description": "Get the payment URL and USDC wallet address for paying for an audit."
    }
  ]
}
